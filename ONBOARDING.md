# نقشهٔ آنبوردینگ تدریجی G-camp

اصل مسیر این است که هر قابلیت بعد از رسیدن کاربر به اکشن مرتبط آموزش داده شود؛ نه در یک تور ابتدایی و اجباری.

| لحظه | سطح نمایش | پیام/اکشن | شرط تکمیل |
| --- | --- | --- | --- |
| ورود اول به تمرکز | راهنمای درون کارت تایمر | مدت را انتخاب کن و «شروع مطالعه» را بزن | ساخت موفق جلسه در سرور (`timer_started`) |
| پایان اولین جلسهٔ پاداش‌دار | داخل پنجرهٔ نتیجه | هر ۱۵ دقیقهٔ تأییدشده = ۱ XP + ۱ سکه؛ XP روی رتبهٔ هفتگی اثر دارد | بستن/ادامه از نتیجه (`rewards_explained`) |
| بعد از فهم پاداش | شیت آماده‌سازی اختیاری | اجازهٔ Web Push و افزودن PWA به صفحهٔ اصلی | اقدام یا «فعلاً نه» برای هر دو گزینه |
| اولین ورود به رده‌بندی | کارت زمینه‌ای داخل صفحه | بازهٔ هفت‌روزه، رقابت هم‌سطح و تب دوستان | زدن «فهمیدم» (`leaderboard_explained`) |
| اولین ورود به اتاق مأموریت | کارت زمینه‌ای داخل صفحه | زمان بازشدن، فعال‌شدن از فردا و استقلال پاداش اعضا | زدن «فهمیدم» (`mission_rooms_explained`) |

## اولویت پاپ‌ها

۱. نتیجهٔ جلسه و توضیح پاداش  
۲. تکمیل اجباری اطلاعات دانش‌آموز بعد از روز اول  
۳. شیت اختیاری اعلان و نصب  
۴. پیشنهاد اختیاری اتصال بله/تلگرام

نتیجهٔ جلسه داخل state تایمر است. سه مورد بعدی در `AppShell` صف می‌شوند تا هیچ دو پاپی هم‌زمان روی هم نیفتند. گام‌های تکمیل‌شده در `User.onboardingHints` ذخیره می‌شوند تا با رفرش یا ورود از دستگاه دیگر تکرار نشوند.

## قواعد مزاحمت

- قبل از اولین مطالعه هیچ اجازهٔ مرورگر یا معرفی تب دیگری درخواست نمی‌شود.
- رد کردن اعلان یا نصب باعث درخواست خودکار دوباره نمی‌شود؛ هر دو از پروفایل قابل دسترسی‌اند.
- راهنمای صفحه‌ها بخشی از layout همان صفحه است و تعامل کاربر را مسدود نمی‌کند.
- کاربر قدیمی که تور قبلی را کامل کرده در migration به‌عنوان تکمیل‌شده ثبت می‌شود.
