# نقشه راه توسعه (Roadmap) — فاز ۱۳ به بعد

> فازهای ۱ تا ۱۲ تکمیل شده‌اند (به [PROJECT.md](PROJECT.md) مراجعه کنید). این سند کارهای باقی‌مانده بر اساس تطبیق با PRD رسمی است.
>
> ترتیب فازها بر اساس **اولویت محصولی**: اول حلقه اصلی گیمیفیکیشن کامل شود، بعد آنبوردینگ، سپس تلگرام و در نهایت پنل ادمین.

---

## فاز ۱۳: تکمیل حلقه گیمیفیکیشن (Critical) ✅ تکمیل شد

> بدون این فاز، چرخه «خرید ماموریت → موفقیت → جایزه → مدال → ارتقا» که قلب محصول است کار نمی‌کرد. **اکنون کامل و تست‌شده است** (`npm run test:gamification`).
>
> فایل‌های کلیدی: `lib/mission.ts` (چرخه عمر ماموریت + recalc سطح)، `lib/gamification.ts` (جدول سطوح OR-of-AND).

### ۱۳.۱ اصلاح منطق ماموریت
- [x] فعال شدن ماموریت **از روز بعد** با `activatesAt` در `UserMission` (وضعیت `pending` → `active`).
- [x] پنجره ۷ روزه: `activatesAt` تا `expiresAt`.

### ۱۳.۲ بررسی تکمیل و اعطای جایزه ماموریت
- [x] `processUserMissions(userId)` در `lib/mission.ts`: مجموع `durationMin` در بازه ≥ هدف → تکمیل.
- [x] موفقیت → `completed` + `xpReward` + `UserMedal` + `ActivityLog` نوع `medal_earn`.
- [x] فراخوانی در `study/end` و lazy در صفحه `/missions`.

### ۱۳.۳ منقضی کردن ماموریت (سوختن سکه)
- [x] ماموریت با `expiresAt < now` و `active` → `failed` (lazy check؛ cron در فاز ۱۸).

### ۱۳.۴ اعطای خودکار مدال
- [x] مدال تکرارپذیر هنگام تکمیل ماموریت.

### ۱۳.۵ اصلاح جدول سطوح (`lib/gamification.ts`)
- [x] شرط مدال **«و» (AND)** — همه مدال‌های یک سطح با هم لازم‌اند (اصلاح‌شده طبق بازخورد؛ مثلاً سرآمد ۱ = مدال ۵۰ **و** ۵۳).
- [x] سطح **«الگو فوق‌ستاره»** (۲×مدال ۷۰).
- [x] آستانه XP تازه‌نفس یک‌ستاره = ۸.
- [x] ثبت `level_up` در فید هنگام ارتقا (`recalcUserLevel`).

**خروجی فاز:** حلقه کامل گیمیفیکیشن کار می‌کند و در فید نمایش داده می‌شود. تست: `npm run test:gamification` و `npm run test:levels`.

---

## فاز ۱۴: تکمیل آنبوردینگ مطابق PRD ✅ تکمیل شد

> اسلایدهای welcome (۴ اسلاید + پرسش ساعت)، ماموریت روز اول، هدف +۳۰ دقیقه، و ویدیوی پاداش روزانه پیاده شد. تست: `npm run test:onboarding`.

<details><summary>جزئیات فاز ۱۴ (تکمیل‌شده)</summary>

### ۱۴.۱ اسلایدهای Welcome ورودی
- [x] فیلد `hasSeenIntro` + تور راهنمای درون‌اپ `GuidedTour.tsx` (نسخهٔ فعلی جایگزین اسلایدهای اولیه شده است).
- [x] نمایش یک‌بار در اولین ورود؛ دکمه «شروع کن» → `PATCH /api/profile`.

### ۱۴.۲ پرسش میانگین ساعت مطالعه گذشته
- [x] فیلد `pastAvgStudyHours` + پرسش در آخرین اسلاید welcome.
- [x] مبنای محاسبه هدف روزانه آنبوردینگ.

### ۱۴.۳ ماموریت ویژه روز اول
- [x] `getDay1MissionHours` (۲-۴ ساعت بر اساس میانگین، نصف اگر بعد از ۱۷).
- [x] پیام و پیشرفت هدف روز در `components/dashboard/DailyMissionCard.tsx`.

### ۱۴.۴ پاداش ویدیویی + تایید روز
- [x] **روز فقط با تکمیل کامل هدف تایید می‌شود** (`onboardingStepMinutes`)، ویدیوی پاداش آن‌وقت باز می‌شود.
- [x] هر روز +۳۰ دقیقه نسبت به روز قبل.

### ۱۴.۵ هدف فردا = امروز + نیم ساعت
- [x] در `GoalSettingModal` هدف فردا نمایش داده می‌شود.

</details>

---

## فاز ۱۵: لیدربورد و بورد زنده مطابق PRD ✅ تکمیل شد

> لیدربورد هم‌سطح (شامل تازه‌نفس، رقابت درون سطح) + بورد زنده ۲۰۰ فعالیت. SSE حفظ شد.

### ۱۵.۱ لیدربورد هم‌سطح
- [x] فیلتر فقط بین کاربران **هم‌سطح** (`level` یکسان)، شامل تازه‌نفس.
- [x] کاربر بدون XP این هفته با ۰ در انتها؛ هدر سطح فعلی + تعداد هم‌سطح‌ها.
- [ ] کش Redis (TTL 60s) — موکول؛ فعلاً `force-dynamic`.

### ۱۵.۲ بورد زنده کامل
- [x] افزایش به **۲۰۰ فعالیت** اخیر.
- [x] SSE حفظ شد (Socket.io به فاز تلگرام موکول شد).

---

## فاز ۱۶: قابلیت‌های اجتماعی ✅ تکمیل شد

### ۱۶.۱ مشاهده پروفایل دیگران
- [x] صفحه `/profile/[userId]` (نمایش عمومی: نام، سطح، مدال‌ها، آمار).
- [x] لینک‌کردن آواتارها در لیدربورد (Podium + List) و بورد زنده.

### ۱۶.۲ مشاهده لاگ مطالعه با پرداخت سکه
- [x] مدل `ProfileUnlock` (`viewerId`, `targetUserId`, `expiresAt` = ۱ ساعت).
- [x] باز کردن گزارش مطالعه با ۲۰ سکه → زنجیره، نمودار و سشن‌های ۷ روز اخیر.

---

## فاز ۱۷: LMS (فقط آنبوردینگ، چندپایه) + مدیریت محتوا ✅ تکمیل شد

> طبق بازخورد: بخش ویدیوهای درسی حذف شد. فقط ویدیوهای آنبوردینگ داریم که هرکدام می‌توانند **چند پایه هدف** داشته باشند (`grades String[]`؛ خالی = همه پایه‌ها). نقش `admin` با `npx tsx scripts/make-admin.ts 0912...`.

### ۱۷.۱ فیلتر ویدیوی آنبوردینگ بر اساس پایه
- [x] صفحه `/videos`: فقط ویدیوهای آنبوردینگ (روز ۱-۶، قفل تا تکمیل روز) و متناسب با پایه کاربر.
- [x] فیلتر: `grades` خالی (همه) یا شامل پایه کاربر. ویدیوی پاداش در `study/end` هم همین فیلتر را دارد.

### ۱۷.۲ پنل ادمین مدیریت ویدیو
- [x] route group `(admin)` با محافظت نقش (`getAdminSession` در layout + هر API).
- [x] CRUD کامل: `/admin/videos`, `/admin/videos/new`, `/admin/videos/[id]`.
- [x] **انتخاب پایه چندانتخابی** (مثلاً «دوازدهم + فارغ‌التحصیل» یا «دهم + یازدهم»)؛ هیچ انتخاب = همه پایه‌ها.
- [ ] آپلود مستقیم فایل + transcode به HLS (آروان/Mux) — موکول به آینده؛ فعلاً URL دستی.

---

## فاز ۱۸: زیرساخت — Job Scheduler و Pooling ✅ تکمیل شد

### ۱۸.۱ Job Scheduler
- [x] `lib/jobs.ts` با `runScheduledJobs()` — پردازش سراسری ماموریت‌ها (فعال‌سازی، تکمیل+جایزه، انقضا).
- [x] endpoint امن `POST /api/cron/run` با `Authorization: Bearer <CRON_SECRET>` (قابل اتصال به Vercel Cron / cron خارجی).
- [x] تست end-to-end: ماموریت منقضی → `failed`، ماموریت کامل → `completed` + مدال + XP.
- [x] اتصال cron واقعی روی VPS از طریق `/app/league/run-cron.sh` و crontab (جزئیات در `DEPLOYMENT.md`). اجرای دستی نیز ممکن است:
  ```bash
  curl -X POST localhost:3000/api/cron/run -H "Authorization: Bearer $CRON_SECRET"
  ```

### ۱۸.۲ Connection Pooling
- [x] تنظیم Pool در `@prisma/adapter-pg` (`max=DB_POOL_MAX`، idle/connection timeout) برای جلوگیری از خروج از کانکشن در ترافیک بالا.

---

## فاز ۱۹: پنل ادمین و CRM (فاز ۵ PRD) ✅ تکمیل شد

> داشبورد آمار، قیف/churn، لیدربورد سطوح، و اکسپورت لید (با لید داغ) ساخته شد. صفحات: `/admin` (داشبورد)، `/admin/leads`، `/admin/leaderboard`.

### ۱۹.۱ داشبورد آمار
- [x] متریک‌ها: کل کاربران، ثبت‌نام امروز، فعال ۲۴ ساعت و ۷ روز، مجموع جلسات.

### ۱۹.۲ تحلیل قیف و نرخ ریزش (Churn)
- [x] قیف ۴ مرحله‌ای: ثبت‌نام → اتمام روز اول → تکمیل لید → ورود به هفته دوم + نرخ ریزش بین مراحل.

### ۱۹.۳ نمای لیدربورد سطوح
- [x] `/admin/leaderboard`: تاپ ۵ هر سطح (XP هفت روز اخیر) به تفکیک ۵ سطح.

### ۱۹.۴ مدیریت و اکسپورت لید
- [x] `/admin/leads`: جدول لیدها + برچسب **لید داغ** (پروفایل کامل + روز ۳ آنبوردینگ).
- [x] خروجی **CSV** (با BOM برای Excel فارسی): همه لیدها یا فقط لیدهای داغ — `/api/admin/leads/export`.
- [ ] تزریق خودکار لید داغ به CRM خارجی — نیازمند CRM واقعی؛ موکول.

### ۱۹.۵ مدیریت پیام و فالوآپ
- [x] پنل قانون‌های اعلان با تریگر زمان‌بندی‌شده، نسبی و رویدادی، شرط، سگمنت، quiet hours و cooldown.
- [x] ارسال از کانال بله و Web Push + اجرای دستی/تست برای ادمین.
- [ ] اتصال به CRM خارجی و گردش‌کار اپراتور انسانی — نیازمند سرویس مقصد.

---

## فاز ۲۰: اصلاحات منطق، امنیت و رقابت (بازخورد بازبینی محصول) ✅ تکمیل

> نتیجه بازبینی کامل محصول. این فاز پیش از تلگرام اولویت گرفت چون مستقیماً روی حلقه اصلی درگیری کاربر و سلامت رقابت اثر دارد.

### ۲۰.۱ امنیت و صحت تایمر
- [x] اعتبارسنجی `POST /api/study/tick`: فقط برای جلسه باز خود کاربر، بر اساس زمان سپری‌شده واقعی سرور (هر ۱۵ دقیقه فقط ۱ پاداش)، سقف = مدت انتخابی تایمر. رفع پاداش دوباره (tick + end).
- [x] Pause واقعی: ثبت `pausedAt`/`pausedSec` سمت سرور (`/api/study/pause` و `/api/study/resume`)؛ زمان pause از مدت جلسه و XP کسر می‌شود.

### ۲۰.۲ قانون ساعت ورود روز اول
- [x] ورود قبل از ۱۷ → هدف کامل (۲-۴ ساعت)؛ ۱۷ تا ۲۱ → نصف (حداقل ۱ ساعت)؛ بعد از ۲۱ → فقط ۱ ساعت.
- [x] هدف روز اول هنگام پاسخ به پرسش میانگین **snapshot** می‌شود (`day1GoalMinutes`) تا در طول روز تغییر نکند.
- [x] از روز دوم: هدف بر اساس میانگین اعلامی کاربر + روزی ۳۰ دقیقه.

### ۲۰.۳ رقبای نزدیک و مینی‌لیدربورد
- [x] «رقبای نزدیک» داشبورد: بر اساس **XP هفتگی** و فقط بین **هم‌سطح‌ها** (اختلاف < ۴۰).
- [x] پیام مینی‌لیدربورد: رتبه بین هم‌سطح‌ها با XP هفتگی + نام سطح واقعی کاربر در متن.

---

## فاز ۲۱: استریک + فید زنده‌تر + جایزه لحظه‌ای ✅ تکمیل

- [x] استریک (زنجیره روزهای متوالی مطالعه): فیلدهای `streak` و `lastStudyDate`، به‌روزرسانی در پایان جلسه و نمایش در داشبورد.
- [x] رویدادهای جدید بورد زنده: شروع تایمر (`timer_start`)، رسیدن به استریک ۳ و ۶ روزه (`streak`) و تکمیل ویدیو (`video_complete`).
- [x] نمایش جایزه لحظه‌ای: انیمیشن شناور «+۱ XP +۱ سکه» روی تایمر در هر tick + نشانگر «جایزه بعدی در X دقیقه».
- [x] خرید مرخصی استریک با ۵۰ سکه (`/api/streak/freeze`).

---

## فاز ۲۲: ویدیو در حلقه روزانه + LMS منعطف ✅ تکمیل با تصمیم محصولی جدید

> تصمیم نهایی فعلی کد: ویدیوی روز جایزهٔ اختیاری است و تکمیل روز فقط به دقیقه‌های مطالعه وابسته است. رقابت نیز هرگز قفل نمی‌شود.

- [x] نمایش هدف مطالعه و وضعیت/پاداش ویدیوی همان روز روی کارت داشبورد.
- [x] جایزه سکه **دوبرابر** (۳۰ به‌جای ۱۵) برای تماشای ویدیو در ۲۴ ساعت اول بازشدن/خرید.
- [x] طول مسیر آنبوردینگ منعطف: تعداد روزها = بیشترین `day` ویدیوهای فعال (پیش‌فرض ۶).
- [x] نمایش ویدیوهای روزهای آینده به‌شکل قفل.
- [x] دکمه CTA قابل‌تنظیم زیر هر ویدیو (`ctaLabel` + `ctaUrl`).
- [x] A/B تست `free` در برابر `paid` با قیمت افزایشی و پنل تحلیل `/admin/analytics`.

---

## فاز ۲۳: پالایش UI و جشن‌ها 🟡 عمدتاً تکمیل

- [ ] حذف آخرین hexهای پراکنده و یکدست‌کردن کامل با توکن‌های `@theme`.
- [x] جشن و انیمیشن CSS: کانفتی، pop-in، feed enter و reward float.
- [x] تایمر جمع‌وجور با فریم داینامیک و شمارش mm:ss.
- [x] پیشرفت سطح در پروفایل همراه شرط مدال‌های `LEVEL_TABLE`.
- [x] `env(safe-area-inset-bottom)` در BottomNav برای PWA در iOS.
- [x] ورودی ساعت دلخواه در GoalSettingModal.
- [x] اصلاح آیکون/برچسب فید و رده‌بندی.

---

## فاز ۲۴: دعوت دوستان + حل Cold Start ✅ تکمیل

- [x] کد دعوت یکتا برای هر کاربر + لینک اشتراک‌گذاری (`/login?ref=CODE`).
- [x] ثبت دوستی دوطرفه هنگام ورود OTP با لینک دعوت.
- [x] تب «دوستان» در لیدربورد: رتبه‌بندی دوستان با XP هفتگی فارغ از سطح.
- [x] Cold start: اگر هم‌سطح‌های کاربر کمتر از ۵ نفر باشند، لیگ آزاد نمایش داده می‌شود.

---

## فاز ۲۵: اتصال ربات تلگرام/بله به حساب موبایلی ✅ تکمیل (بخش اپ)

> بخش داخل اپ کامل و تست‌شده است. سرویس ربات یک پروسه‌ی Node.js **مجزا** است (نیاز به توکن واقعی + استقرار) و در `bot/` اسکفولد و مستند شده.

### ۲۵.۱ سرویس Node.js مجزا
- [x] اسکفولد سرویس مجزا در `bot/` (long-polling، `/start <token>` → اتصال کانال به User موجود). مستندات در `bot/README.md`.
- [ ] Socket.io (بورد زنده) در همین سرویس — موکول (فعلاً SSE در خود اپ کار می‌کند و کافی است).
- [x] ارتباط با اپ از طریق API امن (`POST /api/bot/link` با `BOT_API_SECRET`).

### ۲۵.۲ لینک اتصال
- [x] ثبت‌نام و ورود فقط با موبایل و OTP.
- [x] توکن یکبارمصرف Redis (`lib/bot-link.ts`، TTL ۱۵ دقیقه، مصرف اتمیک با `getdel`).
- [x] deep link ربات → `/start <token>` → اتصال `telegramId`/`baleId` به User موجود، بدون صدور سشن.
- [x] پیشنهاد یک‌باره پس از اولین جلسهٔ ثبت‌شده + مدیریت اتصال‌ها از پروفایل.
- [x] سخت‌سازی ورود: OTP هش‌شده، rate limit شماره/IP، سقف تلاش، JWT نسخه‌دار و ابطال سشن در خروج.
- [x] محافظت CSRF سازگار با دامنهٔ production، reverse proxy و IP شبکهٔ محلی، بدون origin wildcard.

### ۲۵.۳ نوتیفیکیشن هوشمند
- [x] یادآور ۱۵ دقیقه قبل از `nextStudyTarget` (`notifyStudyReminders` در cron؛ از طریق Web Push فاز ۲۶).
- [ ] فالوآپ زمان‌بندی‌شده ادمین (۱۹.۵) — موکول به پنل فالوآپ.

---

## فاز ۲۶: نوتیفیکیشن PWA (Web Push) ✅ تکمیل

- [x] Service Worker (`public/sw.js`) با هندلر `push` و `notificationclick`.
- [x] `lib/push.ts` (web-push + VAPID)، مدل `PushSubscription`، `POST/DELETE /api/push/subscribe`.
- [x] ثبت خودکار SW و subscribe (`components/push/PushRegister.tsx`) + دکمه‌ی صریح اجازه در پروفایل (`NotificationToggle`).
- [ ] انیمیشن‌های Framer Motion — جشن‌های CSS فاز ۲۳ فعلاً کافی‌اند؛ موکول.

---

## فاز ۲۷: نوتیف رقابتی + تورنومنت ✅ تکمیل

### ۲۷.۱ نوتیف‌های رقابتی (`lib/notifications.ts`، از طریق cron)
- [x] افت رتبه هفتگی: «N نفر ازت جلو زدن» با مقایسه‌ی `lastWeeklyRank` (`notifyRankDrops`).
- [x] «زنجیره‌ات امشب می‌سوزه» برای استریک‌دارهایی که امروز مطالعه نکرده‌اند (`notifyStreakAtRisk`).
- [x] یادآور هدف مطالعه (`notifyStudyReminders`).
- [ ] «۳ نفر هم‌سطحت الان مطالعه می‌کنن» / «ویدیوی جدید باز شد» — افزودنی‌های ساده‌ی بعدی.

### ۲۷.۲ تورنومنت (رقابت‌های ویژه ادمین)
- [x] مدل `Tournament` + `TournamentParticipant` (بازه، قوانین سطح، هزینه ورود، جوایز).
- [x] پنل ادمین CRUD (`/admin/tournaments`) + صفحات کاربر (`/tournaments`, `/tournaments/[id]` با لیدربورد زنده و دکمه پیوستن).
- [x] لیدربورد اصلی هفتگی دست‌نخورده؛ تورنومنت رقابت موازی با XP بازه‌ای (`lib/tournament.ts`).
- [x] تسویه‌ی خودکار جوایز پایان تورنومنت در cron (`settleEndedTournaments`) + نوتیف برنده.

## فاز ۲۸: تعامل اجتماعی فید + صندوق ✅ تکمیل

- [x] واکنش‌های 🔥 👏 💪 ❤️ 🎯 روی فعالیت‌های فید؛ هر کاربر روی هر آیتم یک واکنش قابل‌تغییر/حذف دارد.
- [x] به‌روزرسانی خوش‌بینانه در `LiveFeed` و همگام‌سازی با پاسخ سرور.
- [x] صندوق `/inbox`، شمارندهٔ نخوانده در هدر و اعلان Web Push برای واکنش.
- [x] جایزهٔ روزانهٔ تشویق: واکنش به ۵ کاربر متفاوت = ۵ سکه.

## فاز ۲۹: موتور قانون نوتیفیکیشن ✅ تکمیل

- [x] مدل‌های `NotificationRule` و `NotificationLog`.
- [x] تریگرهای `scheduled`، `relative` و `event`، شرط‌های AND، سگمنت و template variable.
- [x] cooldown، سقف روزانه، ساعات سکوت، کانال بله و Web Push.
- [x] پنل ادمین CRUD و اجرای دستی/تست قانون‌ها.
- [x] seed قانون‌های پیش‌فرض بازگشت، هدف، استریک، رتبه، سطح و مدال.

## فاز ۳۰: A/B دسترسی به ویدیو ✅ تکمیل

- [x] تخصیص تصادفی `free` یا `paid` در اولین ورود.
- [x] سکهٔ اولیه و قیمت افزایشی روزانه برای گروه paid.
- [x] خرید ویدیو، ثبت `purchasedAt` و شروع پنجرهٔ پاداش سریع از زمان خرید.
- [x] داشبورد تحلیل `/admin/analytics` برای بازدید، مطالعه، فعالیت و پیشرفت دو گروه.

## فاز ۳۱: ماموریت روزانه و آواتار آپلودی ✅ تکمیل

- [x] ماموریت روزانهٔ ۳/۵/۷/۸/۱۰ ساعته با جایزهٔ سکه و انقضای پایان روز تهران.
- [x] نمایش هم‌زمان وضعیت ماموریت روزانه و هفتگی در داشبورد/بازارچه.
- [x] آواتارهای آماده و آپلود عکس؛ فشرده‌سازی کلاینت و ذخیرهٔ bytes در `AvatarImage`.

## فاز ۳۲: کمپ مأموریت اجتماعی ✅ تکمیل

- [x] ادغام بازارچهٔ خشک مأموریت در مسیر `/mission-rooms` و حفظ `UserMission` به‌عنوان منبع پیشرفت و پاداش فردی.
- [x] گروه‌بندی کاربران با هدف و بازهٔ یکسان، رتبه و پیشرفت زنده بر پایهٔ زمان مطالعهٔ تأییدشده.
- [x] انتخاب مأموریت هفتگی در جمعه و شروع کمپ از شنبه؛ مأموریت روزانه تا پایان همان روز تهران.
- [x] تشویق‌های محدود و از پیش‌تعریف‌شده با Inbox/Web Push؛ بدون پیام خصوصی آزاد.
- [x] حذف «بورد زنده» از ناوبری اصلی و افزودن «کمپ مأموریت» کنار آموزش‌ها؛ فید عمومی از کارت فعالیت صفحه مطالعه در دسترس است.

---

## جمع‌بندی وضعیت

| فاز | وضعیت |
|-----|--------|
| ۱۳ حلقه گیمیفیکیشن | ✅ تکمیل |
| ۱۴ آنبوردینگ | ✅ تکمیل |
| ۱۵ لیدربورد + فید | ✅ تکمیل |
| ۱۶ اجتماعی | ✅ تکمیل |
| ۱۷ LMS + پنل ادمین محتوا | ✅ تکمیل |
| ۱۸ زیرساخت (cron + pooling) | ✅ تکمیل |
| ۱۹ پنل ادمین CRM (آمار/لید/قانون اعلان) | ✅ تکمیل؛ اتصال CRM خارجی باقی است |
| ۲۰ اصلاحات منطق/امنیت/رقابت | ✅ تکمیل |
| ۲۱ استریک + فید + جایزه لحظه‌ای | ✅ تکمیل |
| ۲۲ ویدیو در حلقه روزانه | ✅ تکمیل |
| ۲۳ پالایش UI و جشن‌ها | 🟡 عمدتاً تکمیل؛ چند hex پراکنده و Framer Motion باقی است |
| ۲۴ دعوت دوستان + cold start | ✅ تکمیل |
| ۲۵ اتصال ربات تلگرام/بله | ✅ تکمیل (بخش اپ)؛ اتصال یکبارمصرف به حساب موبایلی و سرویس مجزای تلگرام |
| ۲۶ نوتیفیکیشن PWA (Web Push) | ✅ تکمیل (نیاز به کلید VAPID) |
| ۲۷ نوتیف رقابتی + تورنومنت | ✅ تکمیل |
| ۲۸ واکنش فید + صندوق | ✅ تکمیل |
| ۲۹ موتور قانون نوتیفیکیشن | ✅ تکمیل |
| ۳۰ A/B ویدیو | ✅ تکمیل |
| ۳۱ ماموریت روزانه + آواتار | ✅ تکمیل |
| ۳۲ کمپ مأموریت اجتماعی | ✅ تکمیل |

> تصمیم‌های محصولی فعلی: لیدربورد اصلی همیشه «XP هفت روز گذشته» می‌ماند؛ رقابت‌های بازه‌دار به‌صورت تورنومنت جدا هستند. دیدن ویدیو شرط پیشروی آنبوردینگ نیست و به‌عنوان جایزهٔ اختیاری با مدل A/B free/paid ارائه می‌شود.
